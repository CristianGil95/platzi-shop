import { AkitaFilter } from './akita-filters-store';
import { HashMap, getEntityType } from '@datorama/akita';
/**
 * Helper function to do a default filter
 */
export declare function defaultFilter<E = any, S = any>(value: E | getEntityType<S>, index: number, array: E[] | HashMap<getEntityType<E>>, filter: AkitaFilter<E, S>): boolean;
/**
 * Helper function to do search on all string element
 */
export declare function searchFilter(searchKey: string, inObj: Object): boolean;
/**
 * Helper function to do search in one key of an object
 */
export declare function searchFilterIn(searchKey: string, inObj: Object, inKey: string): boolean;
